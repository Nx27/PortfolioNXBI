@page "/Projects/ColorTD"

<div class="ProjectPage">
  <p>Sorry this page is still in the works.</p>
  <a target="_blank" href="@projectDetails.First().LinkToProject">Feel free to check out the GitHub anyways.</a>
</div>

@code {
  List<ProjectDetails> projectDetails;
  protected override void OnInitialized()
  {
    projectDetails = ProjectDetails.GetByNames("ColorTD");
  }
}
